apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "netflix-helm.fullname" . }}-test-connection"
  labels:
    {{- include "netflix-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "netflix-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
